\chapter{Podsumowanie}

Celem pracy by³o zaprojektowanie oraz zaimplementowanie systemu pozwalaj¹cego na przeprowadzenie badañ wydajnoœciowych na wybranych mechanizmach replikacji systemu bazodanowego PostgreSQL. W ramach realizacji projektu wykonana zosta³a aplikacja serwerowa wykorzystuj¹ca framework Spring oraz konfiguracja czterech metod replikacji: bucardo, pgpool-II, replikacji logicznej oraz strumieniowej.

Projekt systemu zak³ada³ prowadzenie badañ za poœrednictwem tworzonej aplikacji serwerowej, jednak w trakcie wstêpnych testów okaza³o siê, ¿e wprowadza ona opóŸnienia, które wp³ynê³yby na póŸniejsz¹ analizê wyników. W zwi¹zku z tym zdecydowano siê ograniczyæ dzia³anie aplikacji do tworzenia schematu baz danych. Zaprojektowano i zaimplementowano cztery systemy replikacji sk³adaj¹ce siê z dwóch serwerów bazodanowych ka¿dy. Jako serwis hostingowy serwerów u¿yty zosta³ AWS. Serwery pracuj¹ pod kontrol¹ systemu Ubuntu 16.04, a dzia³aj¹ce na nich bazy danych s¹ w wersji 10.4 systemu PostgreSQL. 

Zaprojektowane zosta³y testy wydajnoœciowe oparte o operacje \texttt{SELECT, INSERT} oraz \texttt{UPDATE}. Testy przeprowadzone zosta³y dla zmieniaj¹cej siê liczby aktywnych u¿ytkowników z ze zbioru ${1, 5, 10, 25, 40}$ oraz dla ró¿nego rozmiaru danych w tabelach, od 20 000 do 100 000 rekordów.  Jako program testuj¹cy wydajnoœæ wykorzystany zosta³ Apache JMeter. Z przeprowadzonych badañ zgodnie z oczekiwaniami wynika³o, ¿e odczyt danych za poœrednictwem mechanizmów systemu pgpool-II jest szybszy od pozosta³ych systemów. Rozmiar danych oraz liczba aktywnych u¿ytkowników mia³y wp³yw na czas odczytu. W przypadku operacji zapisu, wyraŸnie wolniejszy od pozosta³ych okaza³ siê system replikacji logicznej. Rozmiar danych oraz liczba po³¹czeñ z baz¹ mia³y znacznie mniejszy wp³yw na wyniki ni¿ mia³o to miejsce dla operacji odczytu. 

Przeprowadzone badania pokazuj¹, ¿e dostêpne systemy replikacji mog¹ wp³ywaæ na wydajnoœæ osi¹gan¹ przez serwery bazodanowe. Projektuj¹c system konieczne jest przeprowadzenie odpowiednich testów i dobranie systemu, który najlepiej nadaje siê do wybranego zastosowania. Systemy, w których konieczne jest czêste pobieranie du¿ej iloœci danych, a ich aktualizacje lub rozszerzanie s¹ rzadsze mog¹ sk³aniaæ siê ku systemowi pgpool, podczas gdy do tworzenia kopii zapasowej bez koniecznoœci edycji lepszy mo¿e byæ mechanizm replikacji strumieniowej. Z punktu widzenia konfiguracji jest on jednak bardziej skomplikowany. Najprostszym systemem, którego konfiguracja wymaga zaledwie kilku komend i zapytañ SQL jest mechanizm replikacji logicznej, jednak uzyskiwane przez niego czasy zapisu s¹ znacz¹co gorsze. Ewentualnym celem dalszych prac mo¿e byæ badanie wp³ywu poszczególnych parametrów konfiguracyjnych ka¿dego z systemów, takich jak dozwolona liczba po³¹czeñ czy rozmiar bloków plików archiwizacyjnych na czasy odpowiedzi.